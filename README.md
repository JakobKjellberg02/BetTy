# BetTy

A simple betting bot simulation with bankroll tracking.

## Usage

Run one simulated bet from the command line:

```bash
python betting_bot.py --balance 100 --stake 10 --odds 2.5 --result win
```

## Tests

```bash
python -m unittest discover -s tests -v
```
